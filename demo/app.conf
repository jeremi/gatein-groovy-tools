application {
    name = "testapp"
}
environments {
    production {
        server {
            url = "http://www.exoplatform.com" //not / at the end
            repository = "repository"
            workspace = "groovy-rest"
            login = "root" //this should probably put somewhere else
            password = "gtn" //this should probably put somewhere else
            autoload = true
        }
    }
    development {
        server {
            url = "http://localhost:8080"
            repository = "repository"
            workspace = "groovy-rest"
            login = "root" //this should probably put somewhere else
            password = "gtn" //this should probably put somewhere else
            autoload = true
        }
    }
}